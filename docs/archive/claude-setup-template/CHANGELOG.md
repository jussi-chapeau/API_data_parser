# Changelog

One entry per deploy to production — date, what shipped, why it matters, one line each. Full detail
lives in the commit messages (`git log`); this is the skim version, not a duplicate of them.

## YYYY-MM-DD

- **[Feature/fix name]** (`commit-sha`) — one line: what shipped and why it matters to the user or
  the team, not a restatement of the diff.
